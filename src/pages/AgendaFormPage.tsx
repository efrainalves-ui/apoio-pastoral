import { ArrowLeft, CalendarPlus, TriangleAlert } from 'lucide-react'
import { type FormEvent, useCallback, useEffect, useMemo, useState } from 'react'
import { Link, useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { categoryForm, resolveTitle } from '../agenda/categoryForm'
import { AgendaService, findAgendaConflicts, isMonday } from '../agenda/service'
import { AGENDA_CATEGORIES, AGENDA_CATEGORY_LABELS, CEREMONY_CHECKLISTS, categoryDefaults, emptyCeremonyDetails, endFollowingStart, isCeremonyCategory, localDateTime, type AgendaCategory, type AgendaEventEntity, type AgendaEventInput, type CeremonyDetails } from '../agenda/types'
import { useAuthVault } from '../auth/AuthVaultContext'
import { Button } from '../components/ui/Button'
import { Card } from '../components/ui/Card'
import { Field } from '../components/ui/Field'
import { DistrictService } from '../district/service'
import type { ChurchEntity } from '../district/types'
import { PeopleService } from '../people/service'
import type { PersonEntity } from '../people/types'
import { SermonService } from '../sermons/service'
import type { SermonEntity } from '../sermons/types'
import { EvangelismPlanningService } from '../evangelism/service'

const agenda = new AgendaService(); const districts = new DistrictService(); const sermonsService = new SermonService(); const peopleService = new PeopleService(); const evangelism = new EvangelismPlanningService()
function initialInput(startAt?: string): AgendaEventInput {
  const defaults = categoryDefaults('visit', startAt ? new Date(startAt) : new Date())
  const validStart = startAt && Number.isFinite(new Date(startAt).getTime()) ? startAt : defaults.startAt
  const endAt = localDateTime(new Date(new Date(validStart).getTime() + 60 * 60_000))
  return { title: '', category: 'visit', churchId: null, location: '', address: '', visitTarget: 'none', sermonId: null, sermonSnapshot: null, ceremonyDetails: null, ...defaults, startAt: validStart, endAt, notes: '', mondayException: false }
}
function durationMinutes(input: AgendaEventInput): number { return Math.max(0, Math.round((new Date(input.endAt).getTime() - new Date(input.startAt).getTime()) / 60_000)) }

export function AgendaFormPage() {
  const { account, masterKey } = useAuthVault(); const { eventId } = useParams(); const [searchParams] = useSearchParams(); const navigate = useNavigate(); const [input, setInput] = useState(() => initialInput(searchParams.get('inicio') ?? undefined)); const [events, setEvents] = useState<AgendaEventEntity[]>([]); const [churches, setChurches] = useState<ChurchEntity[]>([]); const [sermons, setSermons] = useState<SermonEntity[]>([]); const [people, setPeople] = useState<PersonEntity[]>([]); const [error, setError] = useState(''); const [outraIgreja, setOutraIgreja] = useState(false); const [busy, setBusy] = useState(false)
  const load = useCallback(async () => { if (!account || !masterKey) return; const district = await districts.getDistrict(account.id, masterKey); const [nextEvents, nextChurches, current, nextSermons, nextPeople] = await Promise.all([agenda.listEvents(account.id, masterKey), district ? districts.listChurches(account.id, masterKey, district.id) : [], eventId ? agenda.getEvent(account.id, masterKey, eventId) : null, sermonsService.list(account.id, masterKey), peopleService.listPeople(account.id, masterKey)]); setEvents(nextEvents); setChurches(nextChurches); setSermons(nextSermons); setPeople(nextPeople); if (current) { const { id: _id, createdAt: _createdAt, updatedAt: _updatedAt, ...data } = current; setInput(data); void _id; void _createdAt; void _updatedAt } }, [account, eventId, masterKey])
  useEffect(() => { void load() }, [load]); const conflicts = useMemo(() => { try { return findAgendaConflicts(input, events, eventId) } catch { return [] } }, [eventId, events, input])
  function set<K extends keyof AgendaEventInput>(key: K, value: AgendaEventInput[K]) { setInput((current) => ({ ...current, [key]: value })) }
  /** Mudar o início leva o término junto, preservando a duração escolhida. */
  function changeStart(novoInicio: string) {
    setInput((current) => ({ ...current, startAt: novoInicio, endAt: endFollowingStart(current.startAt, current.endAt, novoInicio) }))
  }

  function changeCategory(category: AgendaCategory) { setInput((current) => ({ ...current, category, ...categoryDefaults(category, new Date(current.startAt)), sermonId: category === 'preaching' ? current.sermonId : null, sermonSnapshot: category === 'preaching' ? current.sermonSnapshot : null, ceremonyDetails: isCeremonyCategory(category) ? emptyCeremonyDetails(category) : null })) }
  function setCeremony(patch: Partial<CeremonyDetails>) { const category = input.category; if (!isCeremonyCategory(category)) return; const empty = emptyCeremonyDetails(category); setInput((current) => ({ ...current, ceremonyDetails: { ...(current.ceremonyDetails ?? empty), ...patch } })) }
  function toggleCeremonyPerson(field: 'involvedPersonIds' | 'parentPersonIds', personId: string, checked: boolean) { const ceremony = input.ceremonyDetails; if (!ceremony) return; const next = new Set(ceremony[field]); if (checked) next.add(personId); else next.delete(personId); setCeremony({ [field]: [...next] }) }
  function chooseSermon(id: string) { const sermon = sermons.find((item) => item.id === id); setInput((current) => ({ ...current, sermonId: sermon?.id ?? null, sermonSnapshot: sermon ? { id: sermon.id, title: sermon.title, theme: sermon.theme, mainText: sermon.mainText } : null })) }
  async function submit(event: FormEvent) { event.preventDefault(); if (!account || !masterKey) return; setBusy(true); setError(''); try { const igreja = churches.find(({ id }) => id === input.churchId)?.name ?? input.location; const paraGravar = { ...input, title: resolveTitle(input.category, input.title, igreja) }; const saved = eventId ? await agenda.updateEvent(account.id, masterKey, eventId, paraGravar) : await agenda.createEvent(account.id, masterKey, paraGravar); if (saved.linkedSource) await evangelism.syncFromAgendaEvent(account.id, masterKey, saved); await navigate('/app/agenda') } catch (reason) { setError(reason instanceof Error ? reason.message : 'Não foi possível salvar o compromisso.') } finally { setBusy(false) } }
  const ceremonyCategory = isCeremonyCategory(input.category) ? input.category : null
  const ceremony = ceremonyCategory ? input.ceremonyDetails ?? emptyCeremonyDetails(ceremonyCategory) : null
  const ceremonyPeople = people.filter((person) => !input.churchId || person.currentChurchId === input.churchId)
  const campos = categoryForm(input.category);
  return <div className="page-stack page-narrow"><Link className="text-link back-link" to="/app/agenda"><ArrowLeft />Voltar à agenda</Link><header className="page-hero"><div><p className="eyebrow">Planeje sua rotina</p><h1>{eventId ? 'Editar compromisso' : 'Novo compromisso'}</h1></div><CalendarPlus /></header>{error && <div className="alert alert--error" role="alert">{error}</div>}<form onSubmit={(event) => void submit(event)}><Card title="Dados do compromisso"><div className="form-grid"><label className="field"><span className="field__label">Categoria</span><select className="field__input" value={input.category} onChange={(event) => { const proxima = event.target.value as AgendaCategory; changeCategory(proxima); if (categoryForm(proxima).church === 'none') { set('churchId', null); setOutraIgreja(false) } }}>{AGENDA_CATEGORIES.map((category) => <option key={category} value={category}>{AGENDA_CATEGORY_LABELS[category]}</option>)}</select></label>{campos.church !== 'none' && <label className="field"><span className="field__label">Igreja</span><select className="field__input" required={campos.church === 'required' && !outraIgreja} value={outraIgreja ? 'outra' : input.churchId ?? ''} onChange={(event) => { const escolha = event.target.value; if (escolha === 'outra') { setOutraIgreja(true); set('churchId', null) } else { setOutraIgreja(false); set('churchId', escolha || null) } }}><option value="">{campos.church === 'required' ? 'Selecione' : 'Sem igreja vinculada'}</option>{churches.map((church) => <option key={church.id} value={church.id}>{church.name}</option>)}{campos.otherChurch && <option value="outra">Outra</option>}</select></label>}{campos.church !== 'none' && outraIgreja && <Field label="Nome do lugar" name="agenda-other-church" value={input.location} onChange={(event) => set('location', event.target.value)} maxLength={160} required hint="Fica só neste compromisso; não entra na lista de igrejas do distrito." />}{campos.place && !outraIgreja && <Field label="Local" name="agenda-location" value={input.location} onChange={(event) => set('location', event.target.value)} maxLength={160} />}{campos.place && <Field label="Endereço" name="agenda-address" value={input.address} onChange={(event) => set('address', event.target.value)} maxLength={300} hint={input.churchId ? 'A igreja escolhida já tem endereço no cadastro.' : undefined} />}{campos.title !== 'generated' && <Field label="Título" name="agenda-title" value={input.title} onChange={(event) => set('title', event.target.value)} required={campos.title === 'required'} maxLength={160} hint={campos.title === 'optional' ? 'Opcional: em branco, o compromisso recebe um nome automático.' : undefined} />}<Field label="Início" name="agenda-start" type="datetime-local" value={input.startAt.slice(0, 16)} onChange={(event) => changeStart(event.target.value)} required /><Field label="Término" name="agenda-end" type="datetime-local" value={input.endAt.slice(0, 16)} onChange={(event) => set('endAt', event.target.value)} required hint={`Duração de ${durationMinutes(input)} minutos.`} /></div>{campos.sermon && <label className="field"><span className="field__label">Sermão (opcional)</span><select className="field__input" value={input.sermonId ?? ''} onChange={(event) => chooseSermon(event.target.value)}><option value="">Pregar sem sermão escolhido</option>{sermons.filter(({ status }) => status !== 'archived').map((sermon) => <option value={sermon.id} key={sermon.id}>{sermon.title} · {sermon.mainText}</option>)}</select><small className="field__hint">O histórico recebe um retrato do sermão escolhido.</small></label>}<Field label="Lembrete em minutos" name="agenda-reminder" type="number" min={0} value={input.reminderMinutes ?? ''} onChange={(event) => set('reminderMinutes', event.target.value === '' ? null : Number(event.target.value))} /><label className="field"><span className="field__label">Observações</span><textarea className="field__input" rows={4} value={input.notes} onChange={(event) => set('notes', event.target.value)} maxLength={2000} /></label><div className="check-stack"><label className="confirmation-check"><input type="checkbox" checked={input.allDay} onChange={(event) => set('allDay', event.target.checked)} /><span>Compromisso de dia todo</span></label>{isMonday(input.startAt) && <p className="field__hint">Segunda-feira é seu dia de folga.</p>}</div></Card>{ceremony && ceremonyCategory && <Card title={`Organização · ${AGENDA_CATEGORY_LABELS[ceremonyCategory]}`}><Field label="Responsável" name="ceremony-responsible" value={ceremony.responsible} onChange={(event) => setCeremony({ responsible: event.target.value })} required />{(ceremonyCategory === 'baptism' || ceremonyCategory === 'wedding') && <fieldset className="ceremony-people"><legend>Pessoas envolvidas já cadastradas</legend>{ceremonyPeople.length ? ceremonyPeople.map((person) => <label key={person.id}><input type="checkbox" checked={ceremony.involvedPersonIds.includes(person.id)} onChange={(event) => toggleCeremonyPerson('involvedPersonIds', person.id, event.target.checked)} />{person.name}</label>) : <span>Nenhuma pessoa disponível para a igreja escolhida.</span>}</fieldset>}{ceremonyCategory === 'child_dedication' && <><label className="field"><span className="field__label">Criança</span><select className="field__input" value={ceremony.childPersonId ?? ''} onChange={(event) => setCeremony({ childPersonId: event.target.value || null })} required><option value="">Selecione a criança</option>{ceremonyPeople.map((person) => <option key={person.id} value={person.id}>{person.name}</option>)}</select></label><fieldset className="ceremony-people"><legend>Pais ou responsáveis</legend>{ceremonyPeople.filter(({ id }) => id !== ceremony.childPersonId).map((person) => <label key={person.id}><input type="checkbox" checked={ceremony.parentPersonIds.includes(person.id)} onChange={(event) => toggleCeremonyPerson('parentPersonIds', person.id, event.target.checked)} />{person.name}</label>)}</fieldset></>}<fieldset className="ceremony-checklist"><legend>Checklist da cerimônia</legend>{CEREMONY_CHECKLISTS[ceremonyCategory].map((item) => <label key={item.id}><input type="checkbox" checked={ceremony.checklist[item.id] ?? false} onChange={(event) => setCeremony({ checklist: { ...ceremony.checklist, [item.id]: event.target.checked } })} /><span>{item.label}</span></label>)}</fieldset></Card>}{conflicts.length > 0 && <Card title="Conflitos encontrados"><div className="issue-list">{conflicts.map((conflict, index) => <div key={`${conflict.kind}-${index}`}><TriangleAlert />{conflict.message}</div>)}</div></Card>}<div className="form-actions"><Link className="button button--secondary" to="/app/agenda">Cancelar</Link><Button type="submit" disabled={busy || conflicts.some(({ kind }) => kind !== 'monday_rest') || (isMonday(input.startAt) && !input.mondayException)}>{busy ? 'Salvando…' : 'Salvar compromisso'}</Button></div></form></div>
}
